# Loop #5 — 2026-06-13 12:46 (process feedback)

- **Trigger**: Review 3 ("use worktree per subagent task to avoid conflict").
- **Modules dispatched**: Learning only (process guidance; no code task).
- **Changes**: recorded durable preference — parallel implementation subagents run in their own git worktree (`Agent` `isolation: "worktree"`); read-only subagents exempt. Review 3 → `Reviews/processed/`.
- **Stop reason**: guidance captured; will apply on the next parallel code burst. Critical path remains TASK-...233 (web/map UI), pending network probe.
