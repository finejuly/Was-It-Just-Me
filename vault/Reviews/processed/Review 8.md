Please note that we have about 30-40minutes for development, then I need to submit it. (Github, Web hosting, etc)

---
**TRIAGED (Loop #16):** submission deliverables. Prepared the static web spine to
host anywhere (vite `base: "./"`) + added a GitHub Actions → Pages deploy workflow
(`.github/workflows/deploy.yml`). The remaining step — creating a PUBLIC GitHub repo
and pushing — is the user's call (irreversible: publishes their code), so it is
surfaced as ISSUE-202606131725 with exact copy-paste commands (`gh` is already
authenticated as `finejuly`). No git remote exists yet; nothing was pushed.
