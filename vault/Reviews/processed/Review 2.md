Please use multiple subagents to accelerate the tasks.

---
**Triaged 2026-06-13 (loop #4):** Acted on directly — dispatched 2 implementation subagents in parallel with disjoint file ownership: (A) demo simulator `src/sim/`, (B) time-window + heatmap aggregation `src/core/aggregate.ts`. Both verified (full suite 42/42 pass). Recorded a durable preference to parallelize independent work via subagents in [Preferences.md](../../Loop/Preferences.md). No new task/issue needed; guidance applied.
