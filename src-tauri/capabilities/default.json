{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the Was It Just Me? desktop shell: core app/window/event access plus the global-shortcut plugin. No location or filesystem permissions — privacy stays in the web layer.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:allow-show",
    "core:window:allow-hide",
    "core:window:allow-set-focus",
    "global-shortcut:default"
  ]
}
